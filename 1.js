function compararNumeros(num1, num2) {
  if (num1 >= num2) {
    return true;
  } else {
    return false;
  }
}
print(comparar_numeros(5, 3))  // Saída: True
print(comparar_numeros(2, 7))  // Saída: False
print(comparar_numeros(4, 4))  // Saída: True
